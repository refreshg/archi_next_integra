<!-- last-synced: 2026-09-20, commit: 8ae8331 -->
# Archi ↔ Next — პროდუქტის სტატუსის ინტეგრაცია

როცა **Archi**-ს ბიტრიქსში გარიგება გარკვეულ სტადიაზე გადადის, **Next**-ის ბიტრიქსში
შესაბამისი პროდუქტის სტატუსი ავტომატურად იცვლება: **თავისუფალი / რეზერვი / გაყიდული**.

კავშირი პირდაპირია — Archi-ს ბიზნეს პროცესი უძახებს Next-ის შემომავალ webhook-ს,
შუალედური სერვერის გარეშე. ამ რეპოზიტორიაში დევს ის ინსტრუმენტები, რომლებიც
ინტეგრაციას აწყობს, ამოწმებს და დოკუმენტირებს.

```
Archi: გარიგება -> სტადია "რეზერვი"
   └─▶ ბიზნეს პროცესი: status = reserved
         └─▶ PHP Code აქტივობა
               1. CCrmProductRow::LoadRows  — ბინა გარიგებიდან (მხოლოდ კითხვა)
               2. POST crm.product.list     — ძებნა PROPERTY_546-ით
               3. POST crm.product.update   — სტატუსი + ისტორია
         └─▶ Logstat = UPDATED | REJECTED_SAME | NOT_FOUND | ...
```

---

## დაწყება

### 1. წინაპირობები

- Node.js **18.17+** (გამოიყენება ჩაშენებული `fetch`; გარე პაკეტები არ გვჭირდება)
- ორივე პორტალი **box** (საკუთარ სერვერზე) — `rest` მოდული დაინსტალირებული უნდა იყოს
- **Next**-ის შემომავალი webhook, scope-ით: **`crm`** (`catalog` და `user` არ დასჭირდა)
- Archi-ზე webhook **არ არის საჭირო** — BP-ს PHP კოდი პირდაპირ ბიტრიქსის შიგნით მუშაობს

webhook იქმნება პორტალის ადმინის უფლებით:

- **ღრუბლოვანი:** `დეველოპერებს` (Разработчикам) → `სხვა` → `შემომავალი webhook`
- **box (self-hosted):** `აპლიკაციები` (Приложения) → `ვებჰუკები` → `დაამატე ვებჰუკი` → `შემომავალი`

> სასურველია ცალკე ტექნიკური მომხმარებლით, რომ თანამშრომლის წასვლისას ინტეგრაცია არ გაჩერდეს.

### 2. კონფიგურაცია

```bash
git clone https://github.com/refreshg/archi_next_integra.git
cd archi_next_integra
cp .env.example .env    # PowerShell-ზე: Copy-Item .env.example .env
```

შეავსე `.env` webhook-ების URL-ებით. **ეს ფაილი git-ში არ იდება.**

### 3. ეტაპი 0 — რეკოგნოსცირება

```bash
npm run discover
```

ქმნის:

| ფაილი | შიგთავსი |
|---|---|
| `docs/next-fields.md` | პროდუქტის ველები, dropdown-ების **მნიშვნელობების ID-ები**, კატალოგები, პროდუქტის ნიმუში |
| `docs/archi-fields.md` | გარიგების ველები, ძაბრები, სტადიების `STATUS_ID` |
| `docs/discovery/*.json` | ნედლი პასუხები |

`docs/next-fields.md`-ის ბოლოს გენერირდება `config/mapping.json`-ის მზა ფრაგმენტი — გადაამოწმე და ჩააკოპირე.

### 4. ეტაპი 1 — შესაბამისობის ცხრილი

შეავსე [config/mapping.json](config/mapping.json):

```jsonc
{
  "next": {
    "api": "crm",
    "catalogId": 14,
    "sectionId": 28,                  // Kobuleti Beach Resort
    "archiIdField": "PROPERTY_546",   // UF_ARCHI_ID — ინტეგრაციის გასაღები
    "statusField": "PROPERTY_64",     // სტატუსი
    "statusValues": {
      "free": "თავისუფალი",
      "hold": "უფასო ჯავშანი",
      "reserved": "ფასიანი ჯავშანი",
      "sold": "გაყიდული"
    }
  }
}
```

> ❗ სტატუსი **ტექსტად** იწერება — Next-ის ველი სტრიქონია, არა dropdown. ერთი ასოს შეცდომა
> ჩუმად შექმნის ახალ სტატუსს, ამიტომ მნიშვნელობები მხოლოდ აქ ფიქსირდება.

### 5. ეტაპი 2 — ტესტი ბიზნეს პროცესის გარეშე

```bash
npm run set-status -- --product 1024 --status reserved --dry-run   # მხოლოდ ბეჭდავს
npm run set-status -- --product 1024 --status reserved             # რეალურად ცვლის
```

გასასვლელი კოდები: `0` წარმატება · `2` პროდუქტი ვერ მოიძებნა · `1` სხვა შეცდომა.

სანამ ბიზნეს პროცესს შეეხები, ეს ბრძანება უნდა მუშაობდეს სატესტო პროდუქტზე.

### 6. ეტაპი 3 — ბიზნეს პროცესი Archi-ში

1. Archi → `CRM` → `გარიგებები` → `ავტომატიზაცია` → `ბიზნეს პროცესები` → ახალი, თანმიმდევრული.
2. შექმენი BP ცვლადები: **`status`** (შესატანი), **`Logstat`** და **`Send_log`** (გამოსატანი).
3. `Start` → **ცვლადის შეცვლა** (`status` = `free` / `hold` / `reserved` / `sold`) → **PHP Code** → `End`.
   `hold` = უფასო ჯავშანი, `reserved` = ფასიანი ჯავშანი. სამივე ჯავშანი/გაყიდვა დაცულია.
4. PHP Code-ში ჩასვი [docs/bp/block.php](docs/bp/block.php), პირველ ხაზზე ტოკენი.
   `<?php` ტეგი **არ** დაამატო.
5. შენახვა → **გამოქვეყნება** → გაშვება ავტომატიზაციის წესით საჭირო სტადიებზე.

კოდი თვითონ იღებს ბინას გარიგების პროდუქტების ცხრილიდან — ცალკე ველი არ გჭირდება.

### 7. ეტაპი 4 — შეცდომების დამუშავება BP-ში

`Logstat` ცვლადი ყოველ გაშვებაზე ივსება ერთ-ერთი კოდით:

| კოდი | რა მოხდა |
|---|---|
| `UPDATED` | სტატუსი შეიცვალა |
| `REJECTED_SAME` | უკვე ასეთი იყო, დაცულია — არ გადაიწერა |
| `REJECTED_MULTI` | გარიგებაზე 2+ ბინაა — არაფერი გაიგზავნა |
| `UNCHANGED` | იგივე სტატუსი, ჩაწერა არ დასჭირდა |
| `NO_DEAL` / `NO_PRODUCT` / `NOT_FOUND` | ბინა ვერ დადგინდა ან Next-ში არ არის |
| `BAD_STATUS` | `status` ცვლადში არასწორი მნიშვნელობაა |
| `ERROR_AUTH` / `ERROR_REST` / `ERROR_NETWORK` | ტოკენი / REST / ქსელი |

თითოეული კოდის სრული ახსნა — რა ნიშნავს, სად წერია, რა ქნა: **[docs/CODES.md](docs/CODES.md)**.

`REJECTED_*`-ზე და `ERROR_*`-ზე შეტყობინება პასუხისმგებელთან ჯერ არ არის აწყობილი (გადადებულია).
`Send_log` სრულ ანგარიშს ინახავს, Next-ის მხარეს კი `Interga_History` ველი ყოველ მომართვას იწერს.

---

## პროექტის სტრუქტურა

```
lib/bitrix.js        Bitrix24 REST კლიენტი (call, batch, პარამეტრების კოდირება, retry)
lib/statusUpdate.js  find+update batch-ის ამწყობი — საერთო სკრიპტებისთვის და BP-სთვის
lib/env.js           .env-ის წამკითხველი და ტოკენის დამმალავი
scripts/discover.js  ეტაპი 0 — ველების ამოღება, დოკუმენტაციის გენერაცია
scripts/set-status.js ეტაპი 2 — ხელით ტესტი
scripts/bp-payload.js batch-ის პარამეტრები (webhook-აქტივობისთვის, ახლა აღარ გამოიყენება)
scripts/parse-export.js  ბიტრიქსის ექსპორტის პარსერი
scripts/import-products.js  კატალოგის იმპორტი Next-ის სექციაში
docs/bp/block.php    PHP კოდი, რომელიც Archi-ს ბიზნეს პროცესში ზის
config/mapping.json  ველებისა და სტატუსების შესაბამისობა (საიდუმლო არაფერია)
```

### კატალოგის იმპორტი

ბიტრიქსის პროდუქტების ექსპორტი (`.xls`, სინამდვილეში HTML ცხრილი) → Next-ის სექცია:

```bash
node scripts/parse-export.js files/PRODUCT_*.xls              # ველების ანგარიში
node scripts/import-products.js files/PRODUCT_*.xls           # სატესტო რეჟიმი
node scripts/import-products.js files/PRODUCT_*.xls --execute # რეალური იმპორტი
```

იდემპოტენტურია — `PROPERTY_546`-ით ამოწმებს უკვე ატვირთულს. სამიზნე სექცია:
`config/mapping.json` → `next.sectionId`. დეტალები: [docs/IMPORT-KOBULETI.md](docs/IMPORT-KOBULETI.md).

---

`npm run check` — ყველა ფაილის სინტაქსის შემოწმება.
`MAPPING_FILE=<path>` — სხვა კონფიგზე გაშვება (სატესტო ფიქსტურებისთვის).

---

## რა სად იწერება

| პორტალი | ველი | ვინ წერს |
|---|---|---|
| Next | `PROPERTY_64` სტატუსი | ორივე ბიზნეს პროცესი |
| Next | `PROPERTY_547` ისტორია | ორივე |
| Archi | `PROPERTY_1702` ისტორია | ორივე |
| Archi | `PROPERTY_429` სტატუსი | **მხოლოდ Next-ის BP** |
| Archi | სია **228** `Archi_Next_log` | n8n workflow |

**ყველა ჩაწერა `crm.product.update`-ით ხდება, ზუსტი `id`-ით.** ფილტრით ჩაწერა არსად არ არის —
ბიტრიქსს ასეთი მეთოდი პროდუქტებისთვის არ გააჩნია, ამიტომ ჯგუფური გადაწერა ტექნიკურად შეუძლებელია.
დეტალები: [docs/CODES.md](docs/CODES.md) → `REJECTED_UNSAFE`, და DECISIONS D-21.

---

## შედარება — n8n workflow

`n8n/compare-statuses.json` ადარებს ორივე პორტალის სტატუსებს. **მხოლოდ კითხვა.**

იმპორტი: n8n → `Import from File` → ტოკენები ჩასვი ორ Code ნოუდში
(`ARCHI_TOKEN` და `NEXT_TOKEN` ადგილმჭერების ნაცვლად).

| სვეტი | რა |
|---|---|
| `აიდი` | Archi-ს პროდუქტის ID |
| `ბინის_ნომერი`, `სართული` | ბინის იდენტიფიკაცია |
| `Archi_სტატუსი`, `Next_სტატუსი` | ორივე მხარის სტატუსი |
| `შედეგი` | ემთხვევა / არ ემთხვევა / აკლია Next-ში / Archi-ში არ არის |

ბოლო ნოუდი „შეჯამება" ერთ სტრიქონად აბრუნებს რაოდენობებს.

გრაფიკით გასაშვებად `გაშვება` ნოუდი Schedule Trigger-ით ჩაანაცვლე.

---

## დოკუმენტაცია

| ფაილი | რაზეა |
|---|---|
| [docs/PRD.md](docs/PRD.md) | ბიზნეს პრობლემა, მომხმარებლები, მიღების კრიტერიუმები |
| [docs/SPEC.md](docs/SPEC.md) | ველების დონის სპეციფიკაცია, REST კონტრაქტი, standard-first ცხრილი |
| [docs/PLAN.md](docs/PLAN.md) | განხორციელების გეგმა ეტაპებად |
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) | კომპონენტები და მონაცემთა ნაკადი |
| [docs/DECISIONS.md](docs/DECISIONS.md) | რატომ არის ინტეგრაცია ასე აწყობილი (ADR) |
| [docs/ARCHI-BP-SETUP.md](docs/ARCHI-BP-SETUP.md) | **Archi-ს მხარეს ხელით აწყობის ინსტრუქცია** |
| [docs/IMPORT-KOBULETI.md](docs/IMPORT-KOBULETI.md) | სექცია 28-ის იმპორტი — ველების რუკა და ღია საკითხები |
| [docs/CODES.md](docs/CODES.md) | **შედეგის კოდები — რა რას ნიშნავს და სად წერია** |
| [docs/bp/block.php](docs/bp/block.php) | **PHP კოდი Archi-ს ბიზნეს პროცესისთვის** |
| [docs/bp/block-next.php](docs/bp/block-next.php) | **PHP კოდი Next-ის BP 39 „Archi_integra"-სთვის** |
| [n8n/compare-statuses.json](n8n/compare-statuses.json) | n8n workflow — ორივე კატალოგის შედარება |
| [CLAUDE.md](CLAUDE.md) | ბრძანებები, კონვენციები, წესები |

---

## უსაფრთხოება

- webhook-ის ტოკენი = სრული REST წვდომა იმ მომხმარებლის უფლებებით.
- `.env` და `docs/` (discovery-ის გამოსავალი) `.gitignore`-შია.
- ტოკენი ჩანს BP-ს რედაქტორში Archi-ს ადმინებისთვის — პერსონალის ცვლილებისას გააკეთე როტაცია
  (Next-ზე ძველი webhook წაშალე, ახალი შექმენი, ჩაასწორე `.env` და BP-ს აქტივობის URL).

## ცნობილი შეზღუდვები

- **retry არ არის.** თუ Next იმ წამს მიუწვდომელია, სტატუსი არ შეიცვლება — მხოლოდ `Logstat`-ში
  დაფიქსირდება `ERROR_NETWORK`. საჭიროებისას middleware-ზე გადასვლა Next-ის მხარის შეცვლას არ მოითხოვს.
- **ჩიხის რისკი.** დაცულ სტატუსს (`უფასო ჯავშანი` · `ფასიანი ჯავშანი` · `გაყიდული`) მხოლოდ მისი დამდები
  მხარე ხსნის. მიტოვებული ჯავშნისთვის ან მფლობელმა უნდა გაუშვას `free`, ან ისტორია გასუფთავდეს (D-18).
- **ისტორიის წაშლა ინტეგრაციას თიშავს.** მფლობელობა `Interga_History`-ზე დგას — ველის გასუფთავებისას
  ორივე მხარე კარგავს უფლებას დაცულ სტატუსზე (D-17).
- **Archi-ზე ჯავშანი ერთია.** ფასიანიც და უფასოც `819 დაჯავშნილი`-ში ჯდება — ეს განზრახაა,
  რადგან Archi ამ ეტაპზე ჯავშანს არ ყოფს. განსხვავება Next-ში და ისტორიაში რჩება (D-22).
- **CLI სამ სტატუსს იცნობს, BP — ოთხს.** `npm run set-status` ვერ წერს `უფასო ჯავშანს`.
- **სიჩქარე.** Archi-ს box-იდან ერთი გაშვება 6.5–7.5 წამია, თუმცა Next ~0.4 წამში პასუხობს.
- **550 `Not In Sale` ბინა** ჯერ არ არის Next-ში (DECISIONS D-10, ღია).
- **ავტომატური ტესტები არ არის.** `npm run check` მხოლოდ სინტაქსს ამოწმებს.
