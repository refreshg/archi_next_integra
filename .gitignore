# საიდუმლოებები — webhook-ის ტოკენი არასდროს უნდა მოხვდეს repo-ში
.env
.env.*
!.env.example

node_modules/
*.bak

# discovery-ის გამოსავალი — ასახავს პორტალის ცოცხალ კონფიგურაციას
# (ველები, პროდუქტების ნიმუშები), ამიტომ ლოკალურად რჩება
docs/next-fields.md
docs/archi-fields.md
docs/discovery/

# წყაროს ექსპორტები — კომერციული მონაცემი (ფასები, ბინები)
files/
